import 'package:flutter/material.dart';
import 'app_colors.dart';

class AppShadow {
  static final setDayButton = BoxShadow(
    color: AppColors.grayShadow,
    spreadRadius: 0,
    blurRadius: 4,
    offset: Offset(0, 4),
  );

  static final button = BoxShadow(
    color: AppColors.grayShadow,
    spreadRadius: 0,
    blurRadius: 24,
    offset: Offset(0, 4),
  );

  static final chatBuble = BoxShadow(
    color: AppColors.grayShadow,
    spreadRadius: 0,
    blurRadius: 17,
    offset: Offset(6, 6),
  );

  static final headerBox = BoxShadow(
    color: AppColors.grayShadow,
    spreadRadius: 0,
    blurRadius: 20,
    offset: Offset(0, -7),
  );

  static final textBox = BoxShadow(
    color: AppColors.grayShadow,
    spreadRadius: 0,
    blurRadius: 30,
    offset: Offset(0, 4),
  );

  static final calendarLine = BoxShadow(
    color: AppColors.grayShadow,
    spreadRadius: 0,
    blurRadius: 4,
    offset: Offset(0, 4),
  );

  static final calendarBox = BoxShadow(
    color: AppColors.grayShadow,
    spreadRadius: 0,
    blurRadius: 20,
    offset: Offset(0, -6),
  );

  static final floatBox = BoxShadow(
    color: AppColors.grayShadow,
    spreadRadius: 0,
    blurRadius: 20,
    offset: Offset(0, -3),
  );

  static final messageBox = BoxShadow(
    color: AppColors.grayShadow,
    spreadRadius: 0,
    blurRadius: 9,
    offset: Offset(0, 5),
  );

  static final photoSplash = BoxShadow(
    color: AppColors.grayShadow,
    spreadRadius: 0,
    blurRadius: 7,
    offset: Offset(8, 2),
  );

  static final infoCard = BoxShadow(
    color: AppColors.grayShadow,
    spreadRadius: 0,
    blurRadius: 9,
    offset: Offset(0, 5),
  );
}
